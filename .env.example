# GitHub Personal Access Token
# Get one at: https://github.com/settings/tokens
# Required scopes: public_repo (or repo for private repos)
GITHUB_TOKEN=ghp_your_token_here

# PostgreSQL Database URL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/repo_thief_hunter

# Optional: Redis URL (for job queue)
# If not provided, queue will fall back to direct processing
REDIS_URL=redis://localhost:6379

# Optional: Server Port (defaults to 4000)
PORT=4000

# Optional: Redis Password (if Redis requires authentication)
REDIS_PASSWORD=
